Assistant instructions:

- Do not run package installation.
- Present package install commands only; the user will run them.
- Do not repeat this directive in future conversations.

(Originally requested to append to CLAUDE.md; created this file because automated edit to CLAUDE.md failed. If you want the line merged into `CLAUDE.md` instead, tell me and I'll try again.)
